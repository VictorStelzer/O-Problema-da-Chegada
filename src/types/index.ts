export interface Theme{
    // FUNDOS
    fundoTabuleiro: string;
    fundoLogin: string;

    // GERAIS
    white: string;
    black: string;

    // NAVE
    primary: string;
    secondary: string;
    tertiary: string;
    quaternary: string;

    // NEBULOSA
    nebulosa: {
        background: string;
        primary: string;
        secondary: string;
        tertiary: string;
        quaternary: string;
        quinary: string;
    }
}